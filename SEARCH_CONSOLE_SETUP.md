# Search Console Setup

1. Verify the domain (`NEXT_PUBLIC_GSC_VERIFICATION` or DNS).
2. Submit `https://your-domain/sitemap.xml`.
3. Monitor coverage for Tamil Nadu service/location URLs first.
4. Expand area-service publishing only after crawl/index health looks stable.
